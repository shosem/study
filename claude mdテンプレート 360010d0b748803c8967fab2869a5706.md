# claude.mdテンプレート

# 友達グループ向け予定管理アプリ

## プロジェクト概要

友達グループで「日程調整 → 予約情報登録」を一気通貫で管理できるWebアプリ。
調整さんやLINEを行き来する手間を解消するのがコンセプト。

## 技術スタック

- **フレームワーク**: Ruby on Rails 7.x
- **データベース**: PostgreSQL
- **認証**: Devise
- **フロントエンド**: Hotwire (Turbo + Stimulus) / ERB
- **スタイル**: Tailwind CSS
- **テスト**: RSpec

## コマンド

bash

`# サーバー起動
bin/rails server

# DBセットアップ
bin/rails db:create db:migrate db:seed

# テスト実行（単体）
bundle exec rspec spec/models/user_spec.rb

# テスト実行（全体）
bundle exec rspec

# ルーティング確認
bin/rails routes | grep <キーワード>

# コンソール
bin/rails console`

## ディレクトリ構成

`app/
├── controllers/
│   ├── application_controller.rb
│   ├── groups_controller.rb
│   ├── events_controller.rb
│   ├── availabilities_controller.rb
│   └── reservations_controller.rb
├── models/
│   ├── user.rb
│   ├── group.rb
│   ├── group_member.rb
│   ├── event.rb
│   ├── availability.rb
│   └── reservation.rb
└── views/
    ├── groups/
    ├── events/
    ├── availabilities/
    └── reservations/`

## データモデル

### users

| カラム | 型 | 説明 |
| --- | --- | --- |
| id | uuid | PK |
| name | string | 表示名 |
| email | string | ログイン用 |
| password_digest | string | Devise管理 |

### groups

| カラム | 型 | 説明 |
| --- | --- | --- |
| id | uuid | PK |
| name | string | グループ名 |
| created_by | uuid | FK → users |

### group_members（中間テーブル）

| カラム | 型 | 説明 |
| --- | --- | --- |
| group_id | uuid | FK → groups |
| user_id | uuid | FK → users |

### events

| カラム | 型 | 説明 |
| --- | --- | --- |
| id | uuid | PK |
| group_id | uuid | FK → groups |
| title | string | イベント名 |
| status | string | adjusting / confirmed |
| confirmed_date | date | 確定した日程 |

### availabilities（日程投票）

| カラム | 型 | 説明 |
| --- | --- | --- |
| event_id | uuid | FK → events |
| user_id | uuid | FK → users |
| candidate_date | date | 候補日 |
| answer | string | ok / maybe / ng |

### reservations（予約情報）

| カラム | 型 | 説明 |
| --- | --- | --- |
| event_id | uuid | FK → events |
| store_name | string | 店名・場所 |
| address | string | 住所 |
| start_time | time | 開始時間 |
| people_count | integer | 人数 |
| memo | text | 備考 |

## MVP機能スコープ

### 実装する機能

- [ ]  ユーザー登録・ログイン（Devise）
- [ ]  グループ作成・一覧表示
- [ ]  グループへのメンバー招待（メールアドレスで検索）
- [ ]  イベント作成（タイトル・候補日の設定）
- [ ]  日程投票（○△×で回答）
- [ ]  投票結果の集計表示
- [ ]  日程確定（管理者が決定）
- [ ]  予約情報の登録・編集・表示

### 対象外（後回し）

- リマインド通知（メール・プッシュ）
- 過去イベントの履歴
- コメント・チャット機能

## 画面一覧

| 画面名 | パス | 説明 |
| --- | --- | --- |
| ログイン | /login | Devise標準 |
| 新規登録 | /signup | Devise標準 |
| グループ一覧 | /groups | 自分が参加中のグループ |
| グループ詳細 | /groups/:id | イベント一覧・メンバー一覧 |
| イベント作成 | /groups/:id/events/new | 候補日を複数設定 |
| イベント詳細 | /events/:id | 投票状況・予約情報の表示 |
| 日程投票 | /events/:id/availabilities | 候補日ごとに○△×を選択 |
| 予約情報登録 | /events/:id/reservations/new | 日程確定後に登録 |

## ルール

- ログインが必要なページは `before_action :authenticate_user!` を必ず設定する
- 他グループのデータは参照・編集できないようにスコープを絞る
- マイグレーション後は必ず `db:migrate` を実行してから動作確認する
- N+1問題を避けるため、関連データは `includes` で取得する